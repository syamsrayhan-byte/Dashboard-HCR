{
  "name": "dashboard-hcr",
  "version": "1.0.0",
  "private": true,
  "description": "HCR.ID Office Dashboard (static HTML)",
  "scripts": {
    "dev": "live-server --port=5173 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
